name: Merge from test to check

on:
  workflow_dispatch:

jobs:
  merge-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        
      - name: Merge test to check
        run: |
          git checkout origin/check
          git merge origin/test
          git push origin check
